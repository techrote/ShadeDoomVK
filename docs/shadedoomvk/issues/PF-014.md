# PF-014 — Sprite/portal state correctness repair pack

## Objective

Repair the confirmed sprite/portal-state correctness defects from the audit after PF-009/PF-010 establish explicit sprite orientation and render-context contracts.

## Scope / required work

- Preserve/reproduce the sprite ceiling-clip sentinel defect in `HWSprite::PerformSpriteClipAdjustment`; fix the `top` sentinel/fallback logic with targeted floor/ceiling/3D-floor fixtures.
- Replace `HWSkyInfo` raw `memcmp` equality with semantic field equality/hash/comparison that cannot be influenced by object padding, while preserving sky identity/dedup behavior.
- Audit the PF-009/PF-010 extraction boundaries for directly adjacent mirror/portal state defects revealed by fixtures; fix only source-proven/reproduced issues and file materially separate findings independently.
- Ensure mirror/plane-mirror handedness, portal recursion and sprite visibility/frame state remain compatible.
- Preserve image/state equivalence outside the assigned fixes.

## Non-goals

- No explicit sprite TBN/POM/shadow feature.
- No portal architecture redesign.
- No broad sprite cleanup beyond the reproduced correctness issues.

## Dependencies

PF-009, PF-010.

## Concurrency guidance

May overlap PF-012/PF-013/PF-015 if files do not conflict. Do not overlap PF-009 implementation on the same sprite-state code.

## Required context

Read `AGENTS.md`, PF dependency evidence, `rag/01-RENDERER-EXECUTION-MAP.md`, `rag/07-PORTAL-COORDINATE-SPACES.md`, `rag/10-KNOWN-TRAPS-DORMANT-PATHS.md` and current sprite/portal/sky code.

## Autonomous implementation prompt

Complete PF-014 autonomously. Preserve pre-fix clipping/sky identity fixtures, implement semantic repairs on top of PF-009/PF-010 contracts, validate mirror/portal compatibility and unaffected output, update RAG, then branch→PR→checks→merge→verify `master`→ledger→close on genuine acceptance.

## Acceptance criteria

- Ceiling clipping fallback uses consistent sentinel semantics and fixes the reproduced edge case without regressing normal/3D-floor clipping.
- `HWSkyInfo` identity is based on explicit semantic fields, not raw padding bytes, with equivalent intended dedup behavior.
- Mirror/portal/sprite context fixtures remain correct.
- Any adjacent discovered defect is either reproduced/fixed in-scope because inseparable or recorded separately.
- Required CI passes; PR merged and verified.

## Verification

Sprite floor/ceiling/3D-floor clipping cases including sentinel fallback; repeated construction/comparison of identical/different sky info; linked portal/mirror/plane-mirror sprite fixtures; PF equivalence outside deliberate fixes.

## Expected artifacts

Clipping and sky-identity fixes; regression fixtures; RAG/trap updates; evidence/ledger entry.

## Blocking / stopping conditions

Stop if changing equality would alter a compatibility-relevant sky field whose intended semantics are unknown, or if clipping behavior cannot be distinguished from deliberate legacy compatibility. Record the decision/research need instead of normalizing behavior by intuition.

# PF-019 — Pipeline/resource micro-performance and dormant-path cleanup

## Objective

Harvest remaining low-risk renderer overhead after the major PF refactors: improve canonical pipeline/cache lookup, avoid work/resources belonging solely to provably dormant paths, and accept shader/worker micro-optimizations only when measured and output-equivalent.

## Scope / required work

- Measure pipeline/shader/render-pass lookup and compilation scheduling after PF-006; use canonical typed hashes/maps where they materially reduce lookup cost without changing cache identity.
- Audit creation/use of Z-min/max/light-tile resources and associated descriptors/pipelines. Where the tiled-light path is disabled/dormant, conditionally avoid allocations/dispatch preparation that have no active consumer, while preserving easy re-enable/research seams for SDVK-009.
- Audit render/resource begin-frame work for other clearly unused dormant allocations introduced by inherited experimental paths; require proof of no active consumer before gating them.
- Inspect generated SPIR-V/compiler output before hand-changing repeated dynamic-light vector/distance calculations; accept source micro-optimization only if generated code/performance improves and output remains equivalent.
- Review pipeline/texture worker queue overhead for small measured improvements that do not change ordering/lifetime semantics.
- Record every adopted/rejected micro-optimization with measurement so future agents do not repeatedly rediscover compiler-neutral ideas.

## Non-goals

- No visible quality reduction, resolution/precision/filter changes or light-count reduction.
- No activation/redesign of tiled/clustered lighting; SDVK-009 owns architectural research.
- No broad render-graph rewrite.
- No speculative source golfing without measured generated-code/runtime benefit.

## Dependencies

PF-005, PF-006, PF-007, PF-017, PF-018.

## Concurrency guidance

This is a late PF optimization issue. Start only after its dependencies are accepted; it intentionally consumes their stable contracts. It may run alongside documentation-only preparation for PF-020, not overlapping implementation in its owned paths.

## Required context

Read `AGENTS.md`, all dependency evidence, `rag/08-VULKAN-PIPELINE-CAPABILITIES.md`, `rag/09-POSTPROCESS-HDR-FUTURE-SEAMS.md`, `rag/10-KNOWN-TRAPS-DORMANT-PATHS.md`, validation contract and current generated shader/pipeline/resource behavior.

## Autonomous implementation prompt

Complete PF-019 autonomously. Profile first; adopt only measured output-equivalent micro-performance/resource wins. Prove dormant resources have no active consumer before gating allocation. Inspect SPIR-V before shader-source micro-tuning. Preserve easy future tiled-light research seams. Dedicated branch → benchmarks/equivalence/docs → PR → required checks → merge → verify `master` → RAG/ledger → close.

## Acceptance criteria

- Any pipeline/cache lookup change preserves PF-006 key identity and shows measured benefit or is omitted.
- Resources/work skipped as dormant are proven unused in the active configuration and reappear correctly when their owning path/capability is enabled/tested.
- Any shader micro-change has generated-code/runtime evidence and PF numerical/image equivalence.
- No quality/precision/resolution/filter/light-count policy changes.
- Adopted changes improve at least one representative workload without material regression elsewhere; rejected ideas are documented.
- Required CI passes; PR merged and verified.

## Verification

Warm/cold pipeline lookup/compile tests; render-buffer/resource allocation counters with dormant path disabled/enabled where possible; SPIR-V diff/disassembly and GPU/CPU timings for shader candidates; PF full equivalence subset.

## Expected artifacts

Measured lookup/resource/shader/worker micro-optimizations; rejection notes; benchmarks/equivalence evidence; updated Vulkan/HDR/trap RAG; ledger entry.

## Blocking / stopping conditions

Do not gate a resource whose consumer status is ambiguous. Do not merge a shader micro-change merely because source looks cheaper. Reject changes with no measurable benefit or any semantic/image drift.
